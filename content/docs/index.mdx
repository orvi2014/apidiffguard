---
title: Overview
description: Monitor APIs, review diffs, and schedule checks with APIDiffGuard.
---

APIDiffGuard watches your endpoints, captures baselines, and flags breaking response changes before they hit production.

## What you can do

- Capture a known-good response as a **baseline**
- Run **checks** from the console or on a **schedule** (Starter+)
- Review **diffs** with severity (breaking / warning / info)
- Import endpoints from **OpenAPI** / Swagger UI
- Alert the team when contracts drift (Slack, Discord, webhooks)

A published [REST API](/docs/api) is on the roadmap. The [CLI](/docs/cli) (`apidiff check`) works today for local and CI JSON diffs.

## Start here

| Guide | Purpose |
| --- | --- |
| [Quickstart](/docs/quickstart) | Workspace → endpoint → baseline → check |
| [Concepts](/docs/concepts) | Baselines, diffs, severity, ignore rules |
| [OpenAPI import](/docs/openapi-import) | Bulk import from JSON, YAML, or URL |
| [CLI](/docs/cli) | `apidiff check` for files / URLs in CI |
| [REST API](/docs/api) | Programmatic API status (coming soon) |

Prefer the [Quickstart](/docs/quickstart) and console for production monitoring today.
